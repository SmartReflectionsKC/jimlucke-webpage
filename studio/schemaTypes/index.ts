import { fieldNote } from './fieldNote';
import { photo } from './photo';
import { gallery } from './gallery';
import { blockContent } from './blockContent';

export const schemaTypes = [fieldNote, photo, gallery, blockContent];
